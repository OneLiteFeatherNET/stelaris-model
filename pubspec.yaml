name: stelaris_models
description: Shared API models for the Stelaris frontend.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.13.1 <4.0.0"
  flutter: "^3.47.1"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  material_ui: ^1.0.1
  vulpes_data:
    git:
      url: https://github.com/OneLiteFeatherNET/vulpes-minecraft-dart.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.16.0
  freezed: ^4.0.0
  json_serializable: ^6.14.1

flutter:
